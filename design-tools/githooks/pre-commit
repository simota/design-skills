#!/bin/sh
# Budgets enforced by a mechanism rather than by remembering.
# Install with: make hooks
set -e
python3 "$(git rev-parse --show-toplevel)/design-tools/validate.py"
python3 "$(git rev-parse --show-toplevel)/design-tools/figures_check.py"
